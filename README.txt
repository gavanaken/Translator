To run the test suite for the translator, use the "test_translator" python script.
Unfortunately, you can't just use "Run As" on it, since this is not a "Python" project :-(

So, open a terminal and use the commands

	cd ~/cs245/Workspace/Translator
	python test_translator.py

